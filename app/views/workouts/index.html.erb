<h1>Put cool shit here, Nevin:</h1>

<ul>
  <% @workouts.each do |workout| %>
    <li>
      <h4>Date:</h4> <%= workout.date %>
      <h4>Workout:</h4> <%= simple_format(workout.body) %>
      <h4><%= link_to "Notes", edit_workout_path(workout), class: "btn btn-custom", id: "edit_notes_link", remote: true %></h4>
      <div class="black">
        <%= render 'notes', workout: workout %>
      </div>
      <hr>
    </li>
  <% end %>
</ul>
